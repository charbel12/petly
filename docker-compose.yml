services:
  postgres:
    image: postgres:16-alpine
    container_name: petly-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: petly
      POSTGRES_PASSWORD: petly
      POSTGRES_DB: petly
    ports:
      - "5432:5432"
    volumes:
      - petly_pg_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U petly -d petly"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  petly_pg_data:
